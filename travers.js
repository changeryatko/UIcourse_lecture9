function Travers(pattern) {
  var bodyEls = document.getElementsByTagName("body");
  var match1 = /^[#][a-zA-Z0-9]+/g,
      match2 = /^[.][a-zA-Z0-9]+/g,
      match3 = /^[#][a-zA-Z0-9]+\s[.][a-zA-Z0-9]+/g,
      match4 = /^[a-z0-9]+[.][a-zA-Z0-9]+/g;

  // if Regexp pattern #id

  // if Regexp pattern #class

  // if Regexp pattern .class elements in #id 

}